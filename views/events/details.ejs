<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/components.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <% if (locals.showNavbar !== false) { %>
        <%- include('../partials/navbar') %>
    <% } %>
    
    <main class="main-content">
<div class="container">
    <% if (error) { %>
        <div class="alert alert-error">
            <i class="fas fa-exclamation-circle"></i>
            <%= error %>
        </div>
    <% } %>
    
    <% if (success) { %>
        <div class="alert alert-success">
            <i class="fas fa-check-circle"></i>
            <%= success %>
        </div>
    <% } %>

    <div class="event-detail-container">
        <div class="event-detail-header">
            <div class="event-title-section">
                <h1 class="event-title"><%= event.name %></h1>
                <span class="event-status status-<%= event.status %>">
                    <%= event.status.charAt(0).toUpperCase() + event.status.slice(1) %>
                </span>
            </div>
            
            <div class="event-actions">
                <% if (canRegister && !isRegistered) { %>
                    <form action="/student/events/<%= event._id %>/register" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-user-plus"></i>
                            Register for Event
                        </button>
                    </form>
                <% } else if (isRegistered) { %>
                    <span class="registered-badge">
                        <i class="fas fa-check-circle"></i>
                        Registered
                    </span>
                <% } %>
                
                <% if (canApprove && event.status === 'pending') { %>
                    <div class="admin-actions">
                        <form action="/admin/events/<%= event._id %>/status" method="POST" style="display: inline;">
                            <input type="hidden" name="comments" value="">
                            <button type="submit" name="status" value="rejected" class="btn btn-danger">
                                <i class="fas fa-times"></i>
                                Reject
                            </button>
                        </form>
                        <form action="/admin/events/<%= event._id %>/status" method="POST" style="display: inline;">
                            <input type="hidden" name="comments" value="">
                            <button type="submit" name="status" value="approved" class="btn btn-success">
                                <i class="fas fa-check"></i>
                                Approve
                            </button>
                        </form>
                    </div>
                <% } %>
                
                <% if (canManage) { %>
                    <div class="organizer-actions">
                        <% if (event.status === 'approved') { %>
                            <a href="/club/events/<%= event._id %>/settle" class="btn btn-outline">
                                <i class="fas fa-receipt"></i>
                                Settle Event
                            </a>
                        <% } %>
                    </div>
                <% } %>
            </div>
        </div>

        <div class="event-detail-content">
            <div class="event-main-info">
                <div class="event-description">
                    <h3>About This Event</h3>
                    <p><%= event.description %></p>
                </div>
                
                <div class="event-collaborators">
                    <h3>Organizing Clubs</h3>
                    <div class="club-list">
                        <div class="club-item primary">
                            <i class="fas fa-star"></i>
                            <span><%= event.club?.name || 'Unknown Club' %> (Host)</span>
                        </div>
                        <% if (event.collaborators && event.collaborators.length > 0) { %>
                            <% event.collaborators.forEach(collab => { %>
                                <div class="club-item">
                                    <i class="fas fa-handshake"></i>
                                    <span><%= collab.club?.name || 'Unknown Club' %> (<%= collab.role %>)</span>
                                </div>
                            <% }) %>
                        <% } %>
                    </div>
                </div>
                
                <% if (event.adminComments) { %>
                    <div class="admin-comments">
                        <h3>Admin Comments</h3>
                        <div class="comments-box">
                            <p><%= event.adminComments %></p>
                        </div>
                    </div>
                <% } %>
            </div>
            
            <div class="event-sidebar">
                <div class="event-info-card">
                    <h3>Event Details</h3>
                    <div class="info-list">
                        <div class="info-item">
                            <i class="fas fa-calendar"></i>
                            <div>
                                <strong>Date</strong>
                                <span><%= new Date(event.date).toLocaleDateString() %></span>
                            </div>
                        </div>
                        
                        <div class="info-item">
                            <i class="fas fa-clock"></i>
                            <div>
                                <strong>Time</strong>
                                <span><%= event.startTime %> - <%= event.endTime %></span>
                            </div>
                        </div>
                        
                        <div class="info-item">
                            <i class="fas fa-map-marker-alt"></i>
                            <div>
                                <strong>Venue</strong>
                                <span><%= event.venue?.name || 'TBA' %></span>
                            </div>
                        </div>
                        
                        <div class="info-item">
                            <i class="fas fa-dollar-sign"></i>
                            <div>
                                <strong>Budget</strong>
                                <span>₹<%= event.budget?.toLocaleString() || 'N/A' %></span>
                            </div>
                        </div>
                        
                        <div class="info-item">
                            <i class="fas fa-users"></i>
                            <div>
                                <strong>Registered</strong>
                                <span><%= event.attendees?.length || 0 %> participants</span>
                            </div>
                        </div>
                        
                        <div class="info-item">
                            <i class="fas fa-user-tie"></i>
                            <div>
                                <strong>Organizer</strong>
                                <span><%= event.organizer?.name || 'Unknown' %></span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <% if ((isRegistered || canManage) && event.status === 'approved') { %>
                    <div class="event-actions-card">
                        <h3>Event Actions</h3>
                        <div class="action-buttons">
                            <a href="/chat/event/<%= event._id %>" class="btn btn-primary btn-block">
                                <i class="fas fa-comments"></i>
                                Join Event Chat
                            </a>
                        </div>
                    </div>
                <% } %>
                
                <% if (event.receiptUrl) { %>
                    <div class="settlement-info-card">
                        <h3>Settlement</h3>
                        <div class="settlement-status">
                            <i class="fas fa-check-circle"></i>
                            <span>Event Completed</span>
                        </div>
                        <a href="<%= event.receiptUrl %>" target="_blank" class="btn btn-outline btn-sm">
                            <i class="fas fa-receipt"></i>
                            View Receipt
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>
    </main>
    
    <% if (locals.showNavbar !== false) { %>
        <%- include('../partials/footer') %>
    <% } %>
    
    <!-- Toast Notifications -->
    <div id="toast-container" class="toast-container"></div>
    
    <script src="/js/main.js"></script>
    <script src="/js/components.js"></script>
    <script src="/js/notifications.js"></script>
</body>
</html>